/**
 ******************************************************************************
 * @file    Bsp_Usart.h
 * @brief   USART/UART 底层硬件驱动模块头文件
 * @note    提供串口初始化函数声明以及接收缓冲区大小的宏定义。
 ******************************************************************************
 */

#ifndef _BSP_USART_H_
#define _BSP_USART_H_

#include <stdint.h> /* 系统标准库建议使用尖括号 */

/* ==========================================
 * 宏定义 (Macros)
 * ========================================== */

/**
 * @brief 定义串口 DMA 接收缓冲区的最大长度
 * @note  当前设定为 64 字节，可根据实际通信协议的单帧最大长度进行调整。
 * 如果后续有超长指令包，请同步修改此宏。
 */
#define RX_BUFFER_SIZE  64

/* ==========================================
 * 函数接口声明 (Function Prototypes)
 * ========================================== */

/**
 * @brief  USART 硬件初始化及 DMA 接收配置
 * @note   配置串口使用 DMA 方式接收数据，并开启空闲中断 (IDLE line detection)
 */
void Bsp_Usart_Init(void);

#endif /* _BSP_USART_H_ */
